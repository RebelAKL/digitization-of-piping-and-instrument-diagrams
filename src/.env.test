DEBUG=true

SYMBOL_DETECTION_API=http://inference-endpoint
SYMBOL_DETECTION_API_BEARER_TOKEN=bearer_token

# Storage (local/test GCS bucket)
GCS_BUCKET_NAME=test-pnid-bucket
GCP_PROJECT=my-test-project

# Database (connect via Cloud SQL proxy in test)
GRAPH_DB_TYPE=mysql
CLOUD_SQL_INSTANCE_CONNECTION_NAME=my-test-project:us-central1:test-instance
DB_USER=testuser
DB_PASSWORD=testpassword
DB_NAME=testdb

PORT=8080
